---
phase: 22-visual-architecture-rewrite
plan: 13
type: execute
wave: 3
depends_on: ["22-02"]
files_modified:
  - src/pages/TheoryLessons.tsx
  - src/pages/TheoryLessonDetails.tsx
  - src/pages/Bagruts.tsx
  - src/pages/BagrutDetails.tsx
  - src/pages/Rehearsals.tsx
  - src/pages/RehearsalDetails.tsx
  - src/pages/Settings.tsx
  - src/pages/ImportData.tsx
  - src/pages/MinistryReports.tsx
  - src/pages/AuditTrail.tsx
  - src/pages/Profile.tsx
  - src/pages/Login.tsx
  - src/pages/ForgotPassword.tsx
  - src/pages/ResetPassword.tsx
autonomous: true

must_haves:
  truths:
    - "All page files use Phosphor Icons — zero lucide-react imports in src/pages/"
    - "Secondary list pages (TheoryLessons, Bagruts, Rehearsals) follow list archetype — no hero zone, flat table"
    - "Management pages (Settings, ImportData, MinistryReports) are compact and operational"
    - "Auth pages (Login, ForgotPassword, ResetPassword) use semantic tokens"
  artifacts:
    - path: "src/pages/TheoryLessons.tsx"
      provides: "Theory list page with Phosphor icons and list archetype"
    - path: "src/pages/Settings.tsx"
      provides: "Management page archetype — compact, operational"
  key_links:
    - from: "src/pages/*.tsx"
      to: "@phosphor-icons/react"
      via: "Phosphor icon imports replacing lucide-react"
      pattern: "@phosphor-icons/react"
---

<objective>
Migrate all remaining page files to Phosphor Icons and apply appropriate page archetypes. Secondary list pages get the list archetype (flat table, no hero). Management pages get the operations console archetype (compact, no decoration). Auth pages get token cleanup.

Purpose: After Plan 10 handles the three main list pages and Plan 12 handles the dashboard, this plan catches ALL remaining pages — ensuring zero lucide-react imports across all pages and consistent archetype application.

Output: 14 remaining page files migrated to Phosphor and archetype-appropriate
</objective>

<execution_context>
@/home/yona279/.claude/get-shit-done/workflows/execute-plan.md
@/home/yona279/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ARCHETYPES.md
@.planning/phases/22-visual-architecture-rewrite/22-RESEARCH.md
@.planning/phases/22-visual-architecture-rewrite/22-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate secondary list and detail pages to Phosphor + archetypes</name>
  <files>src/pages/TheoryLessons.tsx, src/pages/TheoryLessonDetails.tsx, src/pages/Bagruts.tsx, src/pages/BagrutDetails.tsx, src/pages/Rehearsals.tsx, src/pages/RehearsalDetails.tsx, src/pages/AuditTrail.tsx, src/pages/Profile.tsx</files>
  <action>
**Phosphor Icon migration for each file:**
1. Read the file and identify ALL lucide-react imports
2. Replace each import with the Phosphor equivalent using the mapping from research:
   - Lucide names → Phosphor names (always append `Icon`)
   - Import from `'@phosphor-icons/react'`
3. Update ALL JSX usage: `<Plus className="w-4 h-4" />` → `<PlusIcon size={16} weight="fill" />`
   - Phosphor uses `size` prop instead of className for sizing
   - Use `weight="fill"` for prominent icons (buttons, nav), `weight="regular"` for inline/subtle icons
4. Remove the lucide-react import entirely — no lucide imports should remain

**Archetype application for secondary list pages (TheoryLessons, Bagruts, Rehearsals):**
- If these pages use ListPageHero, remove it and replace with compact identity strip (same pattern as Plan 10)
- Toolbar flush with table
- Flat table surface

**Detail pages (TheoryLessonDetails, BagrutDetails, RehearsalDetails):**
- If they use DetailPageHeader, it's already updated from Plan 11
- If they have their own header, apply same identity block pattern

**AuditTrail.tsx:** Management/list hybrid — flat table, compact toolbar, no decoration.

**Profile.tsx:** Detail page variant — use dossier pattern if it shows user profile details.

**RTL icons:** For any directional icons (arrows, chevrons), use `mirrored` prop.
  </action>
  <verify>Run `grep -rn "lucide-react" src/pages/TheoryLessons.tsx src/pages/Bagruts.tsx src/pages/Rehearsals.tsx src/pages/AuditTrail.tsx src/pages/Profile.tsx src/pages/TheoryLessonDetails.tsx src/pages/BagrutDetails.tsx src/pages/RehearsalDetails.tsx` — must return ZERO.</verify>
  <done>All 8 secondary list/detail pages use Phosphor Icons and follow appropriate archetypes.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate management and auth pages to Phosphor + archetype</name>
  <files>src/pages/Settings.tsx, src/pages/ImportData.tsx, src/pages/MinistryReports.tsx, src/pages/Login.tsx, src/pages/ForgotPassword.tsx, src/pages/ResetPassword.tsx</files>
  <action>
**Phosphor Icon migration:** Same process as Task 1 — replace all lucide-react imports with Phosphor equivalents.

**Management page archetype (Settings, ImportData, MinistryReports):**
- Page title + brief description header — no hero, no stats, no illustration
- Dense, functional sections with clear labels
- No Card wrappers around individual settings sections
- Actions positioned at task completion point
- Whitespace as structure (section separation via spacing, not card borders)

**Auth pages (Login, ForgotPassword, ResetPassword):**
- Replace lucide-react icons with Phosphor
- Ensure auth forms use:
  - `rounded` (sharp 2px) on form inputs
  - `bg-primary text-primary-foreground` on submit buttons (black)
  - `border-input` on form field borders
- Auth pages should be clean and minimal — no decorative elements that clash with the new aesthetic

**MinistryReports.tsx:** Has info banner about export endpoints not being available. Keep the banner, just clean its styling.
  </action>
  <verify>Run `grep -rn "lucide-react" src/pages/Settings.tsx src/pages/ImportData.tsx src/pages/MinistryReports.tsx src/pages/Login.tsx src/pages/ForgotPassword.tsx src/pages/ResetPassword.tsx` — must return ZERO.</verify>
  <done>All 6 management and auth pages use Phosphor Icons and follow appropriate archetypes. Zero lucide-react imports in all page files.</done>
</task>

</tasks>

<verification>
Final check:
1. `grep -rn "lucide-react" src/pages/` — ZERO results across ALL page files
2. All secondary list pages have flat tables, no hero zones
3. Management pages are compact and operational
4. Auth pages use semantic tokens and sharp radius
</verification>

<success_criteria>
- Zero lucide-react imports across ALL src/pages/ files
- All pages use Phosphor Icons with appropriate weight
- Secondary list pages follow list archetype
- Management pages follow operations console archetype
- Auth pages use clean, minimal styling with new tokens
</success_criteria>

<output>
After completion, create `.planning/phases/22-visual-architecture-rewrite/22-13-SUMMARY.md`
</output>
