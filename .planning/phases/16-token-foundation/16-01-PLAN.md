---
phase: 16-token-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
autonomous: true

must_haves:
  truths:
    - ":root contains 4 surface elevation vars (--surface-base, --surface-raised, --surface-overlay, --surface-floating)"
    - ":root contains 9-step warm neutral scale (--neutral-50 through --neutral-900)"
    - ":root contains 5-level shadow scale (--shadow-0 through --shadow-4) with warm-tinted rgba values"
    - "All 15 existing :root vars are preserved unchanged"
  artifacts:
    - path: "src/index.css"
      provides: "CSS custom property token layer for surfaces, neutrals, and shadows"
      contains: "--surface-base"
  key_links:
    - from: "src/index.css :root"
      to: "tailwind.config.js boxShadow (plan 16-02)"
      via: "shadow CSS vars referenced by Tailwind var() mapping"
      pattern: "--shadow-[0-4]"
---

<objective>
Add all CSS custom property tokens to the `:root` block in `src/index.css` — surface elevation scale (4 levels), warm neutral color scale (9 steps), and semantic shadow scale (5 levels).

Purpose: Establish the single source of truth for elevation, neutrals, and depth that all subsequent v2.1 phases reference. Pure CSS additions with zero blast radius — no existing styles change.

Output: `src/index.css` `:root {}` block extended with 18 new CSS custom properties.
</objective>

<execution_context>
@/home/yona279/.claude/get-shit-done/workflows/execute-plan.md
@/home/yona279/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-token-foundation/16-RESEARCH.md
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add surface, neutral, and shadow CSS custom properties to :root</name>
  <files>src/index.css</files>
  <action>
Inside the existing `@layer base { :root { ... } }` block in `src/index.css`, ADD the following new CSS custom properties AFTER the existing 15 vars (after `--sidebar-foreground: 30 25% 97%;`). Do NOT modify or remove any existing vars.

Add a blank line separator, then add three groups with comment headers:

**Group 1: Surface elevation scale (4 vars)**
```css
/* Surface elevation scale (Phase 16 — TOKEN-01) */
--surface-base:     hsl(30 25% 97%);
--surface-raised:   hsl(30 20% 99%);
--surface-overlay:  hsl(0 0% 100%);
--surface-floating: hsl(0 0% 100%);
```

Note: `--surface-base` value intentionally matches `--background` (same hsl 30 25% 97%). These surface vars store FULL hsl() values (not raw channels like the shadcn vars above), because they are consumed directly via `var(--surface-base)` in CSS, not via the `hsl(var(...))` pattern.

**Group 2: Warm neutral 9-step scale (9 vars)**
```css
/* Warm neutral scale (Phase 16 — TOKEN-02) */
--neutral-50:  hsl(30 20% 98%);
--neutral-100: hsl(28 18% 95%);
--neutral-200: hsl(26 14% 90%);
--neutral-300: hsl(24 12% 82%);
--neutral-400: hsl(22 10% 68%);
--neutral-500: hsl(20 8% 52%);
--neutral-600: hsl(18 8% 40%);
--neutral-700: hsl(16 8% 30%);
--neutral-800: hsl(15 8% 20%);
--neutral-900: hsl(14 8% 12%);
```

**Group 3: Semantic shadow scale (5 vars)**
```css
/* Semantic shadow scale (Phase 16 — TOKEN-03) */
--shadow-0: none;
--shadow-1: 0 1px 2px 0 rgba(120, 60, 20, 0.06);
--shadow-2: 0 2px 6px -1px rgba(120, 60, 20, 0.10), 0 1px 4px -1px rgba(120, 60, 20, 0.06);
--shadow-3: 0 6px 16px -2px rgba(120, 60, 20, 0.12), 0 3px 8px -2px rgba(120, 60, 20, 0.08);
--shadow-4: 0 16px 40px -4px rgba(120, 60, 20, 0.16), 0 8px 16px -4px rgba(120, 60, 20, 0.10);
```

Shadow warm tint uses `rgba(120, 60, 20, ...)` — an approximation of the coral brand hue at low opacity, per research recommendation.

**IMPORTANT constraints:**
- Insert INSIDE the existing `:root {}` block, not after it
- All 15 existing vars MUST remain unchanged
- Surface vars use full `hsl()` syntax (not raw channels) — they are NOT consumed via Tailwind's `hsl(var(...))` pattern
- Shadow vars use standard `box-shadow` syntax with `rgba()` — these will be consumed via `var(--shadow-N)` in Tailwind config (plan 16-02)
  </action>
  <verify>
1. `grep -c "surface-base\|surface-raised\|surface-overlay\|surface-floating" src/index.css` returns 4
2. `grep -c "neutral-50\|neutral-100\|neutral-200\|neutral-300\|neutral-400\|neutral-500\|neutral-600\|neutral-700\|neutral-800\|neutral-900" src/index.css` returns 10
3. `grep -c "shadow-0\|shadow-1\|shadow-2\|shadow-3\|shadow-4" src/index.css` returns 5
4. `grep -c "\-\-background:" src/index.css` still returns 1 (existing var preserved)
5. `grep -c "\-\-sidebar-foreground:" src/index.css` still returns 1 (existing var preserved)
  </verify>
  <done>
`:root` block contains all 18 new CSS custom properties (4 surface + 9 neutral + 5 shadow) alongside the original 15 vars. No existing styles are changed. The CSS file parses without errors.
  </done>
</task>

</tasks>

<verification>
- `src/index.css` has all 18 new CSS custom properties in `:root`
- All 15 original `:root` vars are unchanged
- The file contains no syntax errors (valid CSS)
- New vars are organized with section comments for maintainability
</verification>

<success_criteria>
The `:root` block in `src/index.css` defines 33 total CSS custom properties: the original 15 semantic tokens plus 18 new Phase 16 tokens (4 surface + 9 neutral + 5 shadow). No existing styles were modified.
</success_criteria>

<output>
After completion, create `.planning/phases/16-token-foundation/16-01-SUMMARY.md`
</output>
